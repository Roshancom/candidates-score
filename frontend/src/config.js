/**
 * Application configuration constants.
 * Centralized in one place to avoid hardcoded strings across components.
 */

// Base URL for all API requests (proxied by Vite dev server in development)
export const API_BASE = '/api';
